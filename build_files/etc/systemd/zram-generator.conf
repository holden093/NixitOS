# AriaOS: zRAM disattivato.
# Lo swap comprime le pagine in RAM, quindi sottrae memoria fisica alle
# inferenze locali (modelli GGUF caricati interamente in RAM). AriaOS usa
# uno swapfile su disco NVMe: vedi ariaos-swapfile.service / var-swapfile.swap.

[zram0]
zram-size = 0
